/**
 * jest.setup.ts  (frontend tests)
 *
 * Extends Jest's `expect` with @testing-library/jest-dom matchers such as
 * `toBeInTheDocument()`, `toHaveAttribute()`, `toHaveTextContent()`, etc.
 */
import "@testing-library/jest-dom";
